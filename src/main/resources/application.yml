server:
  port: 80
master:
  user:
  name: KOL
  email: nit@gmail.com
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/demo
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create  
    mail:    
      host: smtp.gmail.com
      port: 587
      username: kavitasample
      password: sample98
      properties:
       mail:
       smtp:
        starttls:
           enable: true            